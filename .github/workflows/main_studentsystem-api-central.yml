name: Deploy API to Azure (Central US)

on:
  push:
    branches: [ main ]
    paths:
      - 'StudentSystem.API/**'
      - 'StudentSystem.Domain/**'
      - 'StudentSystem.Infrastructure/**'
      - 'StudentSystem.Shared/**'
      - '.github/workflows/main_studentsystem-api-central.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore StudentSystem.API/StudentSystem.API.csproj

    - name: Build
      run: dotnet build StudentSystem.API/StudentSystem.API.csproj --configuration Release --no-restore

    - name: Publish
      run: dotnet publish StudentSystem.API/StudentSystem.API.csproj --configuration Release --no-build --output ./api-publish

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v3
      with:
        app-name: 'studentsystem-api-central'
        publish-profile: ${{ secrets.AZURE_API_CENTRAL_PUBLISH_PROFILE }}
        package: ./api-publish
